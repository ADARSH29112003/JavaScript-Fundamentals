//Question is;
//Let firstNum = 7;
//let secondNum = 5;

// Add both Value, multiply by 4 what results declared and subtract 6 from it from the final value.
//But value should be declared in one line

let firstNum = 7;
let secondNum = 5;

let FinalOutput = (firstNum + secondNum) * 4 - 6;
console.log("FinalOutput = ", FinalOutput);